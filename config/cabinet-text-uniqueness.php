<?php

/**
 * Проверка уникальности текста: шинглы + поиск фрагментов / сравнение с URL.
 */
return [
    'version' => '1.2.0',

    'min_chars' => 200,
    'max_chars' => 50000,

    /** Размер шингла в словах (сравнение страниц) */
    'shingle_size' => 4,

    /** Длина зонда в поиске (длиннее → реже ложный ТОП) */
    'probe_words' => 6,

    /** Сколько фрагментов отправлять в поиск (режим «интернет») */
    'probe_min' => 6,
    'probe_max' => 24,
    /** ~1 зонд на столько символов */
    'chars_per_probe' => 300,

    /** Глубина ТОПа XML на один зонд */
    'serp_depth' => 12,

    /** Сколько уникальных URL докачивать после SERP */
    'fetch_url_max' => 24,

    /**
     * URL из зонда с ≤N результатами в выдаче всегда в приоритете на скачивание
     * (часто это и есть реальная копия, а не «шум» по общим фразам).
     */
    'rare_probe_urls_max' => 3,

    /** Макс. URL в режиме сравнения */
    'compare_url_max' => 10,

    /**
     * Порог «чужого» совпадения: ниже — слабый шум (показываем отдельно, в % не входит).
     */
    'min_web_overlap_pct' => 3,
    'min_web_matched_shingles' => 2,

    'default_engine' => 'yandex',
    'default_yandex_lr' => '213',
    'request_pause_ms' => 80,

    /**
     * Лимит: режим internet = число XML-зондов;
     * режим urls = число страниц для скачивания (мин. 1).
     */
];
